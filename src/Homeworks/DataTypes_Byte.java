package Homeworks;

public class DataTypes_Byte {
     //Create a byte which is 5.

    // Print that byte.

    //  Create a byte which is -4.

    //  Print that byte.

    // Create a byte which holds the max value that byte can store.

    // Print that byte.

    //Create a byte which holds the min value that byte can store.

    // Print that byte.

    public static void main(String[] args) {
        byte b = 5;
        System.out.println(b);
        byte a = 4;
        System.out.println(a);
        byte max = 127;
        System.out.println(max);
        byte min = -128;
        System.out.println(min);
    }










}
